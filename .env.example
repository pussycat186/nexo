# Server Configuration
PORT=5000

# JWT Configuration
JWT_SECRET=change_me_to_a_secure_random_string_at_least_32_characters
JWT_ISSUER=nexo

# Database Configuration (Optional)
# When DATABASE_URL is not set, the app uses SQLite at ./data/nexo.db
# Uncomment the line below to use PostgreSQL instead:
# DATABASE_URL=postgresql://user:password@localhost:5432/nexo_db